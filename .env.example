# Azure OpenAI (copy the values from the llm-pricing project).
# Set these in .env locally and in the Vercel demo project's Environment Variables.
AZURE_API_KEY=
AZURE_RESOURCE_NAME=
AZURE_DEPLOYMENT=
AZURE_API_VERSION=

# Mem0 — a free/internal dev key (app.mem0.ai). Used by the remember/recall tools.
MEM0_API_KEY=

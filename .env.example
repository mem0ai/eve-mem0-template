# Injected by the Mem0 integration from the Vercel Marketplace (scoped to your org + project).
# For local development, run: vercel env pull
MEM0_API_KEY=

# Model access. The default agent model routes through the Vercel AI Gateway.
# Set an AI Gateway key, or link a Vercel project to use VERCEL_OIDC_TOKEN instead.
AI_GATEWAY_API_KEY=
